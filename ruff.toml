line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "B"]
# E501: long lines are pydantic Field(...) descriptions and test assertions;
# rewrapping working code for style is out of scope, so line length is not enforced.
ignore = ["E501"]
